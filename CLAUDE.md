# Claude Code working rules

作業開始前に必ず以下を確認すること。

1. GitHub main を直接参照できるか確認
2. ローカルコピーではなく origin/main を基準に調査
3. 現在の branch と remote を表示
4. 修正前に git diff origin/main を表示

これができない場合は「推測ではなく、アクセス不可」と明示すること。
